{
  "name": "mac-terminal",
  "version": "6.1.0",
  "description": "Control the macOS Terminal app",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/patrik-csak/mac-terminal.git"
  },
  "license": "MIT",
  "author": "Patrik Csak <p@trikcsak.com> (https://patrikcsak.com)",
  "type": "module",
  "exports": {
    ".": {
      "types": "./types/index.d.ts",
      "default": "./source/index.js"
    }
  },
  "files": [
    "source",
    "types"
  ],
  "scripts": {
    "prebuild": "rm -fr types",
    "build": "tsc",
    "lint:check": "eslint",
    "lint:fix": "eslint --fix",
    "prepack": "npm run build",
    "quality:check": "npm run types:check && npm run lint:check && npm run unused:check",
    "quality:fix": "npm run lint:fix",
    "test": "node --experimental-test-module-mocks --test",
    "test:coverage": "npm run test -- --experimental-test-coverage",
    "types:check": "tsc --noEmit",
    "unused:check": "knip"
  },
  "dependencies": {
    "plist": "^5.0.0",
    "run-applescript": "^7.0.0"
  },
  "devDependencies": {
    "eslint": "^10.8.0",
    "eslint-config-xo": "^0.58.1",
    "knip": "^6.29.0",
    "typescript": "^6.0.3"
  },
  "engines": {
    "node": ">=22 <=24"
  },
  "devEngines": {
    "runtime": {
      "name": "node"
    },
    "packageManager": {
      "name": "npm",
      "version": "^11.16"
    }
  },
  "os": [
    "darwin"
  ],
  "allowScripts": {
    "unrs-resolver": false
  }
}
